name: apm
version: 1.0.0
description: Skills for working with APM itself — authoring primitives, package lifecycle, marketplaces, dependencies, and security auditing
author: sproott
# Which agent platforms to deploy to.
# Resolution order: --target flag > this field > auto-detect from filesystem.
# Accepted values: vscode, agents, copilot, claude, cursor, opencode, codex,
# gemini, antigravity, windsurf, kiro, agent-skills, all
targets:
  - claude
  - copilot
dependencies:
  apm: []
  mcp: []
includes: auto
scripts: {}
